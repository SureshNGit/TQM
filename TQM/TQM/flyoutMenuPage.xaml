<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TQM"
             Title="FlyoutMenu"
             x:Class="TQM.flyoutMenuPage">

    <StackLayout>
        <ListView x:Name="listview" x:FieldModifier="public">
            <ListView.ItemsSource>
                <x:Array Type="{x:Type local:flyoutItemPage}">
                    <local:flyoutItemPage Title="Company" ImageSource="company.jpg" TargetPage="{x:Type local:companyPage}" />
                    <local:flyoutItemPage Title="User Management" ImageSource="user.png" TargetPage="{x:Type local:UserPage}" />
                    <local:flyoutItemPage Title="Add Machine" ImageSource="machine.png" TargetPage="{x:Type local:machinePage}" />
                    <local:flyoutItemPage Title="Yarn Count Settings" ImageSource="ycsettings.png" TargetPage="{x:Type local:YCSettings}" />
                    <local:flyoutItemPage Title="Exit" ImageSource="logout.png" TargetPage="{x:Type local:MainPage}" />
                    <!--<local:flyoutItemPage Title="Bluetooth Settings" ImageSource="bluetooth.png" TargetPage="{x:Type local:BluetoothConfig}" />-->


                </x:Array>
            </ListView.ItemsSource>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="33"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Image Source="{Binding ImageSource}"/>
                            <Label Grid.Column="1" 
                                   Text="{Binding Title}" 
                                   FontSize="25"
                                   TextColor="{StaticResource blueColor}"
                                   VerticalTextAlignment="Center"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </StackLayout>
   
</ContentPage>